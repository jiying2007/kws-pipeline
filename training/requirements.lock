# Expected runtime inside the immutable qualification training base image.
# The entire transitive dependency/filesystem state is locked by the OCI base
# image digest; this file asserts the product-facing Python/PyTorch contract.
python==3.12.11
torch==2.13.0
