{
  "schema_version": 1,
  "request_id": "speech-like-diagnostic-bounded-preflight-v1-20260922",
  "trigger_policy": "run-on-protected-main-change",
  "purpose": "governed-product-candidate-training",
  "source_policy": "exact-current-main",
  "reason": "Run only the split-stage bounded product speech-like preflight on exact current main after #224 separated total base collapse from strict development qualification, #225 retained compact threshold/confusion diagnostics, #226 added acoustic-only CTC alignment diagnostics using the exact C frontend and exported quantized model, and #227 retained the already-evaluated calibration threshold trial curves. Preserve train-only acoustic rotation, 12 base epochs with 6 warm-start epochs and 6 refinement epochs, utterance identity, evaluation scene seeds, model/training objectives, threshold grid, replay policy, curriculum, strict 0-FRR/0-FAR gates and qualification semantics unchanged. Refinement may run only when the recall-first selected base source has at least one correct match per configured keyword across calibration plus test; full governed training remains blocked unless existing strict gates pass."
}
