{
  "schema_version": 1,
  "request_id": "speech-like-runtime-correlation-preflight-v1-20260922",
  "trigger_policy": "run-on-protected-main-change",
  "purpose": "governed-product-candidate-training",
  "source_policy": "exact-current-main",
  "reason": "Run only the split-stage bounded product speech-like preflight on exact current main after #231 retained per-epoch loss history and #232 added same-WAV training-surrogate versus exact shipping-runtime correlation. Preserve the current product speech-like base, train-only acoustic rotation, 12 base epochs with 6 warm-start epochs, full bounded hard-negative/adversarial/failure replay semantics, 6 refinement epochs, seeds, augmentation, scene rendering, scoring order, calibrated threshold grid, loss coefficients, wake-pressure policy, curriculum, decoder, gates and qualification semantics unchanged. This request is diagnostic evidence only: quantify whether sampled positives exceed the training decoder-confidence surrogate/runtime threshold yet fail the exact scorer-window runtime match, and retain epoch loss trajectories. Full governed training remains blocked unless the existing per-keyword collapse and qualification gates pass."
}
